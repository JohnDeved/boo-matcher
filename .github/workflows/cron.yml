on:
  workflow_dispatch:
  #schedule:
  #  - cron: '*/10 * * * *'

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run script
        run: |
          echo "${{ secrets.ENV_FILE }}" > .env
          bash ./boo.bash
        
